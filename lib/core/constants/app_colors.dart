import 'package:flutter/material.dart';

class AppColors {
  // ── Backgrounds ──────────────────────────────────
  static const Color background   = Color(0xFF060A0F);
  static const Color surface      = Color(0xFF0E1521);
  static const Color card         = Color(0xFF141E2E);
  static const Color cardBorder   = Color(0xFF1E2D44);

  // ── Primary — Emergency Red ───────────────────────
  static const Color primary      = Color(0xFFFF2D2D);
  static const Color primaryDark  = Color(0xFFCC1A1A);
  static const Color primaryGlow  = Color(0x33FF2D2D);

  // ── Accent — Blue ─────────────────────────────────
  static const Color accent       = Color(0xFF00B0FF);
  static const Color accentDark   = Color(0xFF007EB8);
  static const Color accentGlow   = Color(0x2200B0FF);

  // ── Status Colors ─────────────────────────────────
  static const Color green        = Color(0xFF00E676);
  static const Color greenGlow    = Color(0x2200E676);
  static const Color yellow       = Color(0xFFFFD600);
  static const Color orange       = Color(0xFFFF6D00);

  // ── Text ──────────────────────────────────────────
  static const Color textPrimary  = Color(0xFFEEF2FF);
  static const Color textSecond   = Color(0xFF8A9BB8);
  static const Color textDim      = Color(0xFF4A5568);
  static const Color textWhite    = Color(0xFFFFFFFF);

  // ── Misc ──────────────────────────────────────────
  static const Color divider      = Color(0xFF1E2D44);
  static const Color inputFill    = Color(0xFF0E1521);
  static const Color inputBorder  = Color(0xFF1E2D44);
  static const Color shimmerBase  = Color(0xFF141E2E);
  static const Color shimmerHigh  = Color(0xFF1E2D44);
}
